<h1>Posts</h1>
<%= render "posts/form" %>
<% @posts.each do |post| %>
  <div class="post-wrapper">
    <h2>
      <%= "##{post.id}" %>
      <%= post.title %>
    </h2>
    <p>
      <%= post.body %>
    </p>
    <p>
      <%= post.created_at %>
    </p>
  </div>
<% end %>
